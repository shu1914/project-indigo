# Setup

Needs the ff packages installed.

```bash

## To use <fmt/format.h>
sudo apt install libfmt-dev

## To use <nlohmann/json.hpp>
sudo apt install nlohmann-json3-dev

## For testing
sudo apt install catch2
```